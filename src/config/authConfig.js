/**
 * =================================================================
 *   تنظیمات ورود (Login Credentials)
 * =================================================================
 *
 *   فقط دو مقدار زیر را با اطلاعات دلخواه خودت جایگزین کن.
 *
 *   نکته امنیتی:
 *   این پروژه Frontend-only است (بدون Backend/دیتابیس)، بنابراین
 *   این مقادیر داخل کد ساخته‌شده (build) قابل مشاهده هستند و این
 *   یک قفل ساده برای جلوگیری از دسترسی افراد عادی است، نه یک
 *   سیستم امنیتی واقعی مثل احراز هویت سمت سرور.
 * =================================================================
 */

export const USERNAME = "admin"
export const PASSWORD = "admin123"

/**
 * کلید ذخیره‌سازی وضعیت ورود در localStorage.
 * فقط "true"/"false" در آن ذخیره می‌شود، نه هیچ اطلاعات
 * منابع انسانی یا مالی.
 */
export const AUTH_STORAGE_KEY = "hr-dashboard-auth"
